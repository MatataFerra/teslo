import { FC, useContext, useEffect } from "react";
// import { useMap } from "react-leaflet";
import { PickupContext } from "../../context";

export const LeafletControlGeocoder: FC = () => {
  // const map = useMap();
  // const { pickup } = useContext(PickupContext);
  // useEffect(() => {
  //   map.setView([Number(pickup.latitude), Number(pickup.longitude)], 11);
  // }, [map, pickup.latitude, pickup.longitude]);
  return <>Only working in develop</>;
};
